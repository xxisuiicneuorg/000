name: example

on: [ push, pull_request ]

jobs:

  runner:

    runs-on: ubuntu:20.04 # 选择你想要的主机系统如：ubuntu:20.04 1

    steps:

      - uses: actions/checkout@v3

      - name: collie

        uses: dvkunion/CollieTrickster@main

        with:

          token: b7fceacc32a8e34bb8897f319c8867af  # your token,  It will be safer to use ${{ secrets.token }}, see [https://docs.github.com/actions/security-guides/encrypted-secrets] 
